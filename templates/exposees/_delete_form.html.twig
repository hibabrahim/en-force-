<form method="post" action="{{ path('app_exposees_delete', {'id': exposee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exposee.id) }}">
    <button class="btn">Delete</button>
</form>
